[package]
name = "lazin"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive", "cargo"] }
serde = { version = "1.0", features = ["derive"] }
toml = "1.1.2"
lazin_logger = { path = "../lazin_logger" }
lazin_pipeline = { path = "../lazin_pipeline" }
codespan-reporting = "0.13.1"
sha2 = "=0.10.9"
