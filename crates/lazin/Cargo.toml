[package]
name = "lazin"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = "4.6.1"
serde = { version = "1.0", features = ["derive"] }
toml = "1.1.2"
lazin_logger = { path = "../lazin_logger" }
