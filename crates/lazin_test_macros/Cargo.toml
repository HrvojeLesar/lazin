[package]
name = "lazin_test_macros"
version = "0.1.3"
edition = "2024"
description = "Simple macros for testing and non fatal asserts"
homepage = "https://github.com/HrvojeLesar/lazin"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
quote = "1"
syn = { version = "3", features = ["full"] }

[dev-dependencies]
lazin_test = { path = "../lazin_test", version = "0.1.0" }
